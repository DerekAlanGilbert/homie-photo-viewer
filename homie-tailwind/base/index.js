// => Colors, Screens
const colors = require('./colors')
const screens = require('./screens')
const fontWeight = require('./misc')

// => Extended Styles
const { fontSize, spacing, height, minHeight, minWidth, zIndex, boxShadow } = require('./extended')

module.exports = {
  colors,
  screens,
  fontWeight,
  fontSize,
  spacing,
  height,
  minHeight,
  minWidth,
  zIndex,
  boxShadow
}
